/*  
    Template file: All the new things I learn would be added here
    Git repo (https://github.com/coderManit/CPP-CP) will be updated accordingly
*/
#include<bits/stdc++.h>

#define boost std::ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
#define llint long long int
#define sz(x) (llint)(x).size()
#define all(v) v.begin(), v.end()
#define desc greater<int>()
#define forEach(seq) for(auto &i: seq)
#define _ceil(x, y)  (x + y - 1)/y
#define gout(i) cout << "Case #" << i << ": ";
#define F first
#define S second

using namespace std;

void solve(){
    
}

int main() {
    boost;
    int t; cin>>t;
    while(t--)
        solve();
    return 0;
}
